# /etc/nginx/conf.d/custom.conf

server {
    listen {{ nginx_listen_port | default(80) }};
    server_name {{ server_name }};

    location / {
        root {{ document_root }};
        index {{ index_file }};
    }
}
